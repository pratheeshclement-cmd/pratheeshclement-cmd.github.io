# Render dynamically assigns $PORT in production. For local testing:
PORT=8000

# CORS origins: comma-separated list of allowed origins.
# For production on Render, set:
CORS_ORIGINS=https://pratheeshclement-cmd.github.io
